import { PagePlaceholder } from "@/components/shared/PagePlaceholder";

export default function LoginPage() {
  return (
    <PagePlaceholder
      phase="Phase 2"
      title="Trang đăng nhập sẽ được ghép ở phase kế tiếp."
      description="Form đăng nhập, validate bằng React Hook Form + Zod và flow lưu JWT sẽ được nối vào nền phase 1 này."
    />
  );
}
