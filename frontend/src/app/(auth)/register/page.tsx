import { PagePlaceholder } from "@/components/shared/PagePlaceholder";

export default function RegisterPage() {
  return (
    <PagePlaceholder
      phase="Phase 2"
      title="Trang đăng ký đang chờ implementation."
      description="Auth API service và register form sẽ được dựng ở phase 2 trên cùng các store/provider đã tạo."
    />
  );
}
